app:
  version: ${APP_VERSION:-0.0.1}


database:
  url: x
  username: y
  password: z

spring:
  datasource:
    url: jdbc:postgresql://localhost:32769/devdb
    username: dev
    password: devpassword
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml



springdoc:
  swagger-ui:
    path: /apidocs
